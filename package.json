{
  "name": "cards",
  "version": "1.0.0",
  "description": "cards for component library",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js' --directory --7777 --browser 'FirefoxDeveloperEdition"
  },
  "author": "balaji",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.27.7"
  }
}
